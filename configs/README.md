# Sample configuration files

A sample configuration using a BiLSTM encoder is provided in
[`bilstm.yaml`](bilstm.yaml); fill in the arguments labeled `FIXME` before using
it.

Sample usage:

    neudia fit --config bilstm.yaml

For more examples, see [the tests](../tests).
